# Scaling Potato - Interaction Diagram

## System Architecture Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                         CLIENT LAYER                                │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────────┐        ┌──────────────────┐                 │
│  │  Web Browser     │        │  Socket Client   │                 │
│  │  (index.html)    │        │  (telnet/nc)     │                 │
│  └────────┬─────────┘        └────────┬─────────┘                 │
│           │                           │                            │
│           │ HTTP POST                 │ Raw TCP                    │
│           │ /api/query                │ localhost:9999             │
└───────────┼───────────────────────────┼───────────────────────────┘
            │                           │
            ▼                           ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      SERVER LAYER (Port 8080 & 9999)               │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │               ScalingPotatoApp (Main)                      │  │
│  │  - Initializes services                                    │  │
│  │  - Starts HTTP & Socket servers                            │  │
│  │  - Manages shutdown hooks                                  │  │
│  └────────────────┬──────────────────────────┬────────────────┘  │
│                   │                          │                    │
│         ┌─────────▼──────────┐      ┌────────▼──────────┐        │
│         │   WebServer        │      │   QueryServer     │        │
│         │   (Port 8080)      │      │   (Port 9999)     │        │
│         │                    │      │                   │        │
│         │  ▪ GET /          │      │ ▪ Accept clients   │        │
│         │  ▪ POST /api/query│      │ ▪ Read queries     │        │
│         │  ▪ GET /api/stats │      │ ▪ Send responses   │        │
│         └────┬────┬────┬────┘      └────┬────┬────┬────┘        │
│              │    │    │                │    │    │               │
└──────────────┼────┼────┼────────────────┼────┼────┼───────────────┘
               │    │    │                │    │    │
        ┌──────▼────▼────▼────────────────▼────▼────▼──────┐
        │  BUSINESS LOGIC LAYER                           │
        ├─────────────────────────────────────────────────┤
        │                                                 │
        │  ┌──────────────────┐  ┌──────────────────┐   │
        │  │  NLPService      │  │ DatabaseService  │   │
        │  │                  │  │                  │   │
        │  │ ▪ processQuery() │  │ ▪ saveQueryResult│   │
        │  │ ▪ callOpenAIAPI()│  │ ▪ getStats()     │   │
        │  │ ▪ mockResponse() │  │ ▪ getRecentQueries│  │
        │  │ ▪ escapeJson()   │  │                  │   │
        │  └────────┬─────────┘  └────────┬─────────┘   │
        │           │                     │              │
        └───────────┼─────────────────────┼──────────────┘
                    │                     │
        ┌───────────▼─────────────────────▼──────────────┐
        │  DATA LAYER                                   │
        ├─────────────────────────────────────────────────┤
        │                                                 │
        │  ┌──────────────────┐  ┌──────────────────┐   │
        │  │  OpenAI API      │  │  H2 Database     │   │
        │  │                  │  │  (in-memory)     │   │
        │  │ chat/completions │  │                  │   │
        │  │ gpt-3.5-turbo    │  │ queries table    │   │
        │  │                  │  │ stats view       │   │
        │  └──────────────────┘  └──────────────────┘   │
        │                                                 │
        └─────────────────────────────────────────────────┘
```

---

## Detailed Request/Response Flow

### Web Browser Query Flow

```
1. User enters query in web interface
   │
   ▼
2. Browser sends POST request to /api/query
   │
   ▼
3. WebServer receives HTTP request
   │
   ▼
4. QueryHandler.handle() is called
   │
   ▼
5. Extract JSON query from request body
   │
   ▼
6. Call NLPService.processQuery(query)
   │
   ├─► Check if API key configured
   │
   ├─► YES: Call OpenAI API
   │   │
   │   ▼
   │   Make HTTPS request to OpenAI
   │   │
   │   ▼
   │   Parse JSON response
   │   │
   │   ▼
   │   Return AI response
   │
   └─► NO: Generate mock response
       │
       ▼
       Return mock JSON
   │
   ▼
7. Record processing time
   │
   ▼
8. Call DatabaseService.saveQueryResult()
   │
   ▼
9. Insert into H2 database
   │
   ▼
10. Return JSON response to browser
    │
    ▼
11. Browser displays response & processing time
```

### Socket Server Query Flow

```
1. Client connects to localhost:9999
   │
   ▼
2. QueryServer accepts connection (virtual thread per client)
   │
   ▼
3. ClientHandler reads query from socket
   │
   ▼
4. Send to NLPService.processQuery()
   │
   ▼
5. Get response from NLPService
   │
   ▼
6. Format as JSON response
   │
   ▼
7. Write to socket output stream
   │
   ▼
8. Client receives response
```

---

## Component Interactions

### NLPService ↔ OpenAI API

```
NLPService
    │
    ├─► Check OPENAI_API_KEY from .env
    │
    ├─► If empty/null:
    │   └─► generateMockResponse()
    │       └─► Return: {"response": "Mock...", "timestamp": ...}
    │
    └─► If key exists:
        └─► callOpenAIAPI()
            │
            ├─► Create HTTP connection
            ├─► Set headers: Authorization: "Bearer sk-..."
            ├─► POST to: https://api.openai.com/v1/chat/completions
            ├─► Send JSON body with query
            │
            ▼ (200 OK response)
            │
            ├─► Parse JSON response
            ├─► Extract text from response
            └─► Return to caller
```

### DatabaseService ↔ H2 Database

```
DatabaseService
    │
    ├─► initializeDatabase()
    │   │
    │   ├─► Load H2 driver
    │   ├─► Create connection
    │   └─► CREATE TABLE IF NOT EXISTS queries
    │
    ├─► saveQueryResult(query, response, time)
    │   │
    │   ├─► INSERT INTO queries (query, response, created_at, processing_time_ms)
    │   └─► Commit transaction
    │
    └─► getStats()
        │
        └─► SELECT COUNT(*), AVG(processing_time_ms) FROM queries
```

---

## Virtual Thread Concurrency

```
┌─────────────────────────────────────────────────────┐
│  QueryServer (Port 9999)                            │
│                                                     │
│  Main Thread                                        │
│  ├─► Listen for connections                        │
│  │                                                 │
│  └─► For each connection:                          │
│      │                                             │
│      ├─► Create virtual thread                     │
│      │   (~1KB memory, ~1-2μs creation time)       │
│      │                                             │
│      └─► Launch ClientHandler in virtual thread    │
│          │                                         │
│          ├─► Read query from socket                │
│          ├─► Call NLPService (blocking on I/O)     │
│          │   (Virtual thread suspends, not blocked)│
│          ├─► Wait for OpenAI response              │
│          │   (Virtual thread yields CPU)           │
│          ├─► Send response back to client          │
│          └─► Close connection                      │
│                                                     │
│  Result: 10,000+ concurrent connections            │
│  Memory: ~10 GB with platform threads              │
│          ~10 MB with virtual threads               │
│                                                     │
└─────────────────────────────────────────────────────┘
```

---

## Data Flow Diagram

```
INPUT                PROCESSING               OUTPUT
─────                ──────────               ──────

User Query
    │
    ▼
┌─────────────────┐
│ Web Browser OR  │
│ Socket Client   │
└────────┬────────┘
         │
         ▼
    ┌─────────┐
    │ Server  │
    └────┬────┘
         │
    ┌────▼──────────────────┐
    │ NLPService            │
    │                       │
    │ Check API Key         │
    │   ├─ If YES ──────┐   │
    │   │               │   │
    │   │         ┌─────▼──────────────┐
    │   │         │ OpenAI API         │
    │   │         │ Chat Completion    │
    │   │         └─────┬──────────────┘
    │   │               │
    │   │         AI Generated
    │   │         Response
    │   │               │
    │   └──────┐        │
    │   If NO  ├────────┤
    │          │        │
    │    Mock  │        │
    │  Response│        │
    │          │        │
    └──────────┼────────┘
               │
               ▼
        ┌──────────────────┐
        │ DatabaseService  │
        │                  │
        │ Save to H2 DB    │
        └────────┬─────────┘
                 │
                 ▼
          ┌──────────────────┐
          │ Return Response  │
          │ + Processing Time│
          └────────┬─────────┘
                   │
                   ▼
              Client receives
              formatted JSON
```

---

## Error Handling Flow

```
Error Scenario         Handling                      Result
──────────────────     ────────────────────────      ──────────

API Key Missing    →   Check in NLPService       →   Mock response

API 401 Error      →   Catch IOException         →   "API Error: 401"
(Invalid Key)          Return error message

API Rate Limited   →   Catch IOException         →   "API Error: 429"
(429)                  Return error message

Database Error     →   Catch SQLException        →   Continue execution
                       Log warning               (query still works)

Port Already Used  →   Catch BindException       →   Server startup fails
                       Log error                 (need to clear ports)

JSON Parse Error   →   Catch parsing exception   →   Return 400 error

Connection Timeout →   Catch SocketTimeoutEx.    →   Client disconnect
```

---

## Configuration Flow

```
System Start
    │
    ▼
ScalingPotatoApp.main()
    │
    ├─► loadEnv() reads .env file
    │   │
    │   ├─► OPENAI_API_KEY
    │   ├─► DATABASE_URL
    │   ├─► PORT (9999)
    │   └─► THREAD_POOL_SIZE (50)
    │
    ├─► Create NLPService(apiKey, model)
    │
    ├─► Create DatabaseService(dbUrl, user, pwd)
    │   │
    │   └─► initializeDatabase()
    │       └─► Create H2 schema
    │
    ├─► Create QueryServer(port, threadPool, nlp, db)
    │   └─► server.start()
    │
    ├─► Create WebServer(8080, nlp, db)
    │   └─► server.start()
    │
    ├─► Register shutdown hook
    │   │
    │   └─► On Ctrl+C:
    │       ├─► server.stop()
    │       └─► webServer.stop()
    │
    └─► Wait for shutdown signal
        (lock.wait())
```

---

## Technology Stack Interaction

```
┌──────────────────────────────────────────────────────────────────┐
│                        Application                              │
│                    (Scaling Potato)                             │
└──────────────────────────────────────────────────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
        ▼                     ▼                     ▼
   ┌─────────────┐    ┌─────────────┐    ┌──────────────┐
   │   Java 25   │    │   Project   │    │   SLF4J +    │
   │   (Runtime) │    │   Loom      │    │   Logback    │
   │             │    │ (Virtual    │    │ (Logging)    │
   │ ▪ JDK API   │    │  Threads)   │    │              │
   │ ▪ HTTP Srv  │    │             │    │ ▪ Console    │
   │ ▪ Socket    │    │ ▪ Threads   │    │ ▪ Structured │
   └─────────────┘    │ ▪ Executors │    └──────────────┘
                      └─────────────┘
        │                     │                     │
        └─────────────────────┼─────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
        ▼                     ▼                     ▼
   ┌─────────────┐    ┌──────────────┐    ┌──────────────┐
   │     H2      │    │   OpenAI     │    │    Maven     │
   │  Database   │    │     API      │    │ (Build Tool) │
   │ (In-Memory) │    │              │    │              │
   │             │    │ ▪ gpt-3.5    │    │ ▪ Compile    │
   │ ▪ JDBC      │    │ ▪ REST API   │    │ ▪ Test       │
   │ ▪ SQL       │    │ ▪ JSON       │    │ ▪ Package    │
   │ ▪ Storage   │    │ ▪ HTTP/TLS   │    │ ▪ Execute    │
   └─────────────┘    └──────────────┘    └──────────────┘
```

This diagram should help visualize how all the components interact! 🎯

